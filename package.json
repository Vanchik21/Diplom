{
  "name": "physis",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "frontend": "npm run start --workspace=apps/frontend",
    "backend": "dotnet run --project apps/backend"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
